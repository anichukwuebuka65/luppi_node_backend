This is the backend for my single page app , "luppi", a social media application.

To run , clone the repository , run "npm install" to install dependencies,  

Add your database configurations, run "node index.js" 